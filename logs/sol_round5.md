MINOR: §3.1 の「the split counters on on-fit runs do not change under any of the five levels」は、非 `profile_*` 実行が on-fit ではないため不整合で、`on on-fit` も不自然。例えば以下に修正：

> `SQ_INSTS_VALU` and the base EA counter change with the DPM mode, while the split counters remain zero under all five levels.
